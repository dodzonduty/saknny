import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Primary ────────────────────────────────────────
  static const primary = Color(0xFF1A365D);
  static const primaryContainer = Color(0xFF152C4D);

  // ── Accent ─────────────────────────────────────────
  static const accentYellow = Color(0xFFFBBF24);
  static const accentYellowHover = Color(0xFFF59E0B);

  // ── Backgrounds & Surfaces ─────────────────────────
  static const background = Color(0xFFF8FAFC);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceVariant = Color(0xFFF1F5F9);
  static const surfaceDim = Color(0xFFDAD9DD);

  // ── Text ───────────────────────────────────────────
  static const onBackground = Color(0xFF0F172A);
  static const onSurface = Color(0xFF1A1C1E);
  static const onSurfaceVariant = Color(0xFF475569);
  static const onPrimary = Color(0xFFFFFFFF);

  // ── Status ─────────────────────────────────────────
  static const success = Color(0xFF166534);
  static const successLight = Color(0xFF22C55E);
  static const successContainer = Color(0xFFDCFCE7);
  static const error = Color(0xFFBA1A1A);
  static const errorContainer = Color(0xFFFEE2E2);
  static const warning = Color(0xFFA16207);
  static const warningContainer = Color(0xFFFEF08A);

  // ── Misc ───────────────────────────────────────────
  static const outline = Color(0xFF74777F);
  static const outlineVariant = Color(0xFFC4C6CF);
  static const disabled = Color(0xFF9CA3AF);
  static const disabledContainer = Color(0xFFE5E7EB);
}
